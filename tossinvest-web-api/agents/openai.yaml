interface:
  display_name: "TossInvest Web API"
  short_description: "Query TossInvest read-only stock APIs"
  default_prompt: "Use $tossinvest-web-api to safely query TossInvest read-only web APIs for stock summaries, quotes, filings, news, financials, trading trends, themes, market indices, feed discovery, and screener counts."
