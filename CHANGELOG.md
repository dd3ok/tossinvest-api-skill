# 변경 이력

사용자에게 영향을 주는 주요 변경을 기록합니다. 날짜는 한국시간(KST) 기준이며,
`Unreleased`에는 `main`에 반영됐지만 아직 버전 릴리스로 발행하지 않은 변경을 적습니다.
이전 이력은 릴리스와 PR 기록을 바탕으로 주요 내용만 요약했습니다.

## Unreleased

### 2026-09-07 — API 갱신, HTTP 수정, Python 3.12 단일 지원

#### 지원 환경 변경

- **Python 3.12.x만 지원·검증합니다.** 기존 Python 3.10 지원을 종료하고, CI를 3.12 단일 작업으로 단순화했습니다. Ruff 대상 버전과 스킬·설치 안내·릴리스 체크리스트도 맞췄습니다.
- 전체 테스트 전에 잠금된 `websocket-client==1.9.0`을 설치해 실제 라이브러리 통합 테스트까지 실행합니다. 일반 HTTP 조회는 계속 표준 라이브러리만 사용하며, WebSocket 실행에만 선택 의존성이 필요합니다.

#### API 및 문서 갱신

- 공식 Open API 문서 `1.2.14`의 REST operation 36개·schema 90개와 AsyncAPI operation 10개를 대조했습니다. 로컬 기록에 없던 전체 종목·투자자별 매매·프로그램매매·공매도·신용·대차 관련 GET 경로 6개와 호출 한도·페이징·인증 조건을 참고 문서에 반영했습니다. 공식 OAuth API 정보는 공개 웹 클라이언트와 별도 경계로 관리합니다.
- 공개 페이지의 탭·연결 링크·다음 페이지·정렬·필터와 관련 파라미터를 직접 확인하고, 대표 API의 첫·다음 페이지와 공개 국내 주식 체결 이벤트를 점검했습니다.
- 현재 웹 경로 63개와 로컬 API 표 항목 164개를 정리하고 공식 명세·웹 번들·업데이트 감사 문서를 추가했습니다. 164개에는 중복이 포함되며, 모든 API 조합의 실측 성공을 뜻하지 않습니다. 완전한 과거 명세가 없어 전체 추가·삭제 목록은 확정하지 않았습니다.
- 로컬 업데이트 전략 문서를 Git에서 제외하는 규칙을 추가했습니다.

#### 조회 및 페이징 수정

- 종목 댓글 조회에서 상품 코드 대신 종목 메타데이터의 `guid`를 `subjectId`로 사용합니다. 예를 들어 입력 `A005930`은 `KR7005930003`으로 조회합니다. 기존 `--code` 입력은 유지되며, 응답의 `subjectId`는 실제 GUID를 나타냅니다.
- 종목 통합 조회는 이미 받은 메타데이터를 재사용해 중복 요청을 제거합니다. GUID가 없거나 잘못되면 댓글 요청 전에 실패합니다.
- 댓글·답글의 누락·반복 커서와 여러 페이지에 걸친 커서 순환을 차단하고, 출력 제한으로 페이지를 자를 때 마지막으로 내보낸 댓글 ID를 이어받도록 수정했습니다.
- 뉴스와 AI 상세 조회에서 점이 포함된 지수 코드의 대소문자를 보존합니다.
- 차트 지표의 기간과 Bollinger 편차를 네트워크 요청 전에 검증하고, 연기금 이력 조회를 한 번에 최대 20개 달력연도로 제한합니다.

#### HTTP 및 WebSocket 처리 수정

- 기본 URL의 빈 `?`·`#` 구분자를 요청 전에 차단합니다. 별도로 검증하는 API 경로의 정상 쿼리 파라미터는 유지합니다.
- HTTP 301/302/303/307/308은 `Location`을 파싱하기 전에 차단합니다. 잘못된 IPv6 주소에서도 후속 요청 없이 응답을 닫고, HTTP 오류의 서버 원문이 디버그 출력에 노출되지 않도록 처리했습니다. REST와 게스트 조회가 같은 처리를 사용합니다.
- REST 응답 읽기를 16 MiB, 게스트 응답을 4,096바이트로 제한하고 실패한 응답·소켓의 정리를 보완했습니다.
- WebSocket은 리다이렉트를 따르지 않으며, HTTP 101 연결 승격을 확인한 뒤 게스트 연결값을 담은 STOMP `CONNECT`를 전송합니다.
- 리다이렉트 핸들러를 지연 생성해 `urllib`과 저장소의 `calendar.py` 사이 순환 import를 방지합니다.

#### 검증

- 최종 Python 3.12 CI에서 테스트 **270개 통과, 건너뜀 0개**를 확인했습니다. Ruff, 스크립트 21개의 컴파일·도움말, JSON 예제 3개와 설치 레이아웃·스킬 검증도 통과했습니다.
- 독립 기준 준수·요구사항 리뷰와 GitHub 자동 리뷰를 마쳤고, `main` 머지 후 CI도 통과했습니다. 확인된 수정 사항은 [PR #32](https://github.com/dd3ok/tossinvest-api-skill/pull/32)와 [PR #33](https://github.com/dd3ok/tossinvest-api-skill/pull/33)에 반영했습니다.

상세 근거와 미검증 범위는 [업데이트 점검 기록](references/update-audit-2026-09-07.md),
최종 실행 결과는 [main CI](https://github.com/dd3ok/tossinvest-api-skill/actions/runs/34072778630)를 참고하세요.

### 2026-08-18 — 공개 미국 지수 WebSocket 복구

- 정규장 중 제한된 재확인에서 이벤트를 받은 `COMP.NAI`, `SPX.CBI`, `RGI..VIX`, `SOX.NAI` 네 지수의 구독을 복구했습니다.
- 로그인 요구·미검증 지수는 계속 차단하고, 허용 목록과 문서·회귀 테스트를 갱신했습니다. 당시 테스트 241개가 통과했습니다. [PR #31](https://github.com/dd3ok/tossinvest-api-skill/pull/31)

### 2026-08-13 — 공개 페이지와 페이징·스트림 점검

- 공개 페이지의 경로·탭·링크와 번호·커서 페이징을 재점검하고, 누락된 조회 범위와 요청·응답 기록을 보완했습니다. 커뮤니티·피드 정제, Windows 출력, 잘못된 커서·종목 입력 처리도 개선했습니다. [PR #29](https://github.com/dd3ok/tossinvest-api-skill/pull/29)
- KOSDAQ `QGG01P`와 BTC·ETH·XRP·SOL 스트림 허용 목록을 정리하고 STOMP 프레임을 보완했습니다. 당시 이벤트를 확인하지 못해 잠정 차단한 미국 지수 네 개는 8월 18일 재확인 후 복구했습니다. [PR #30](https://github.com/dd3ok/tossinvest-api-skill/pull/30)

### 2026-08-05 — 산업 API와 스킬 안내 보완

- 현재 산업 정보를 제한된 범위로 조회하는 클라이언트를 추가하고, 산업·채권·웹 경로·WebSocket 점검 결과를 문서화했습니다.
- 스킬의 작업 분기·메타데이터·검증·안전 안내를 정리했습니다. 당시 테스트 220개가 통과했습니다. [PR #28](https://github.com/dd3ok/tossinvest-api-skill/pull/28)

## v1.0.0 — 2026-07-17

- 첫 안정 릴리스로 스킬 이름, 설치 구조, 문서화된 CLI와 로그인·계좌·주문을 제외하는 안전 경계를 공개 계약으로 정리했습니다.
- 시장 통합 검색, 산업 순위·주식/ETF 표, 지수 일별 시세, 라운지 댓글·커뮤니티 랭킹, 스크리너 메타데이터 등 공개 조회 범위를 확장했습니다.
- 국내·미국 주식과 허용된 지수·가상자산형 지수의 공개 WebSocket 클라이언트를 제공했습니다. 임시 게스트 값은 메모리에만 보관하고 구독 수·시간·버퍼를 제한했습니다.
- 당시 Python 3.10·3.12와 테스트 204개를 검증했습니다. 현재 지원 버전은 위 `Unreleased`의 변경 내용을 따릅니다.

[v1.0.0 릴리스 노트](https://github.com/dd3ok/tossinvest-api-skill/releases/tag/v1.0.0)

토스증권 웹 API와 WebSocket은 비공식 인터페이스입니다. 외부 경로·응답 필드·데이터 가용성은 예고 없이 바뀔 수 있습니다.
